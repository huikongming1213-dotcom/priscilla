fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pypdf>=4.0.0
httpx>=0.27.0

# Setup-only (used by scripts/scan_form.py + auto_validate.py — not at runtime).
# Pure-Python, no poppler/system binaries required.
pypdfium2>=4.30.0
pillow>=10.0.0
